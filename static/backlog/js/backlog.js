// $( function() {
//   $( "#sortable" ).sortable();
//   $( "#sortable" ).disableSelection();
// } );

// Sortable.create(simpleList, { /* options */ });

Sortable.create(idea_list, {
  handle: '.fa-grip-vertical',
  animation: 150
});